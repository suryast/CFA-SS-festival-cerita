<div class="container">
	<div class="row">
		<div class="col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2">
			<article>
				<p><a href="/volunteer_index">&larr; kembali ke index</a></p>
				<hr>
				<header>
					<h1>{{data.volunteer.title}}</h1>
					<h5>
						Status relawan: {{{data.volunteer.state}}}
					</h5>
				</header>
				<div class="post">
					{{#if data.volunteer.image}}<img src="{{{cloudinaryUrl data.volunteer.image width=750 height=450 crop='fit' }}}" class="img-responsive">{{/if}}
					{{{data.volunteer.content.extended}}}
				</div>
			</article>
		</div>
	</div>
</div>
